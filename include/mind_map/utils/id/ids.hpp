#pragma once

#include <cstdint>
#include <string>

namespace mind_map {
    using UserId = std::string;
    using SpaceId = std::string;
    using NodeId = std::string;
    using EdgeId = std::string;
    using CommentId = std::string;

    using Version = std::uint64_t;
}
